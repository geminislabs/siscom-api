# Para soporte de compresión Kafka
lz4
# --- Core FastAPI stack ---
fastapi
uvicorn[standard]

# --- Configuración y entorno ---
python-dotenv
pydantic-settings

# --- Base de datos y ORM ---
SQLAlchemy
asyncpg      # PostgreSQL async driver
aiosqlite    # SQLite async driver (for in-memory tests)
psycopg[binary]

# --- Seguridad y autenticación JWT ---
python-jose[cryptography]
passlib[bcrypt]

# --- Seguridad PASETO ---
pyseto

# --- Valkey/Redis (resolución de scope del data token) ---
redis>=5

# --- Kafka Client ---
kafka-python

# --- Logging y utilidades ---
loguru

# --- Métricas ---
aio-statsd

# --- Pruebas y cliente HTTP ---
pytest
pytest-asyncio
httpx

# --- Tipos opcionales y dev helpers ---
types-python-jose
types-passlib

# --- Herramientas de desarrollo y linting ---
# Versiones FIJAS a propósito. El CI instala desde este fichero y ejecuta
# `ruff check` como puerta de calidad: sin fijar, una release nueva de ruff
# puede poner en rojo un PR verde sin que nadie haya tocado código. Pasó con
# ruff 0.16, que estabilizó PLR0917 y empezó a marcar funciones que llevaban
# meses en `develop`. Subir la versión debe ser un cambio deliberado y visible
# en el diff, no un efecto secundario del día en que corre el CI.
ruff==0.16.5
black==26.5.1
mypy
pytest-cov
